@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<ObjectRef.Core.Objects.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">@ObjectRef.ActionLinkExtensions.TagLink(htmlHelper, tag)</div>     
  }
}